server {
  listen 80;
  listen [::]:80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA: cualquier ruta desconocida devuelve index.html (enrutado por Angular).
  location / {
    try_files $uri $uri/ /index.html;
  }
}
